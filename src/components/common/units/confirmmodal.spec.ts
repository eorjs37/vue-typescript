import ConfirmModal from "../ConfirmModal.vue";
import { shallowMount } from "@vue/test-utils"
describe("common modal unit test", () => {
  let wrapper:any = null;

  beforeEach(async () => {
    wrapper = shallowMount(ConfirmModal)
  });
  test("should ", async () => {
        
  });
});